# LinkedIn search result 142

- Query: `"loop engineering"`
- Captured: 2026-06-11T17:40:35Z
- URL: 
- URN: 

## Visible text

Everyone is suddenly talking about "loop engineering." The idea: stop prompting your coding agent one step at a time. Design the loop that prompts it for you. Claude Code already ships the whole loop: → /goal: define a testable end state. A separate fast model grades every turn. The agent that wrote the code never decides the work is finished. → /loop: re-run on a schedule. Hooks and GitHub Actions keep it alive after you close the laptop. → Subagents + worktrees: parallel loops that never collide on files. → State on disk: the model forgets between runs, the repo doesn't. I broke down how one cycle actually executes, from "define done" to "goal cleared," in the visual below. The part most people miss: a green evaluator is a claim, not a proof. The loop removes the typing, not the engineering. You still own the merge. Are you still prompting turn by turn, or already designing loops? hashtag #ClaudeCode hashtag #AgenticAI hashtag #AIEngineering hashtag #LoopEngineering hashtag #SoftwareEngineering
